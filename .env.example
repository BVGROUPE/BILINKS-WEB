# Admin web — full backend (pas de mode démo)
# URL publique de l'API backend (sans slash final) — obligatoire
# Dev local : http://localhost:3000
NEXT_PUBLIC_API_URL=https://backend-butp.onrender.com
NEXT_PUBLIC_API_BASE_URL=https://backend-butp.onrender.com

# JWT admin optionnel (sinon obtenu après /signin avec un compte role ADMIN)
NEXT_PUBLIC_ADMIN_BEARER_TOKEN=

# Déconnexion automatique de l'admin après N minutes d'inactivité (défaut : 10)
NEXT_PUBLIC_ADMIN_IDLE_TIMEOUT_MINUTES=10
